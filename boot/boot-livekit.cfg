default 0
showmenu 1

title Install AOSC OS (USB boot)
    kernel (usb0,0)/boot/kernel
    initrd (usb0,0)/boot/minird.img
    args root=aosc-livekit:CDLABEL=LiveKit quiet splash livekit.boot=base LANG=C.UTF-8

title Install AOSC OS (SATA CD)
    kernel (cd0,0)/boot/kernel
    initrd (cd0,0)/boot/minird.img
    args root=aosc-livekit:CDLABEL=LiveKit quiet splash livekit.boot=base LANG=C.UTF-8

title Install AOSC OS (USB boot, command line only)
    kernel (usb0,0)/boot/kernel
    initrd (usb0,0)/boot/minird.img
    args root=aosc-livekit:CDLABEL=LiveKit quiet splash livekit.boot=base systemd.unit=multi-user.target LANG=C.UTF-8

title Install AOSC OS (SATA CD, command line only)
    kernel (cd0,0)/boot/kernel
    initrd (cd0,0)/boot/minird.img
    args root=aosc-livekit:CDLABEL=LiveKit quiet splash livekit.boot=base systemd.unit=multi-user.target LANG=C.UTF-8
